createCanvas(400,300)
background(0);
strokeWeight(2);
stroke(255);


int (x = 0);
while(x < width) {
  ellipse(x, 25, 25, 25);
  x = x + 40;
}
 
int (x = 0);
while(x < width) {
  ellipse(x, 60, 25, 25);
  x = x + 40;
}

int (x = 0);
while(x < width) {
  ellipse(x, 95, 25, 25);
  x = x + 40;
}


int (x = 0);
while(x < width) {
  ellipse(x, 130, 25, 25);
  x = x + 40;
}

int (x = 0);
while(x < width) {
  ellipse(x, 165, 25, 25);
  x = x + 40;
}

int (x = 0);
while(x < width) {
  ellipse(x, 200, 25, 25);
  x = x + 40;
}

int (x = 0);
while(x < width) {
  ellipse(x, 235, 25, 25);
  x = x + 40;
}

int (x = 0);
while(x < width) {
  ellipse(x, 270, 25, 25);
  x = x + 40;
}

int (x = 0);
while(x < width) {
  ellipse(x, 305, 25, 25);
  x = x + 40;
}

